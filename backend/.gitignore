# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
dist/
build/
*.egg
.eggs/

# Virtual environments
venv/
env/
.venv/

# Local env
.env
.env.local
.env.docker

# Uploads and data
data/uploads/*
!data/uploads/.gitkeep
data/faiss/*.index
data/faiss/*.meta
data/faiss/*.pkl
# M38.4 multimodal embedding prototype 测试样本(runtime 生成,不入 git)
data/m38-4-prototype/*
!data/m38-4-prototype/.gitkeep
# M38.1 follow-up: MinIO dev 数据(volume bind mount / 本地 dev 调试,
# runtime 生成,不入 git — docker compose 容器里自带 /data 持久卷)
data/minio/*
!data/minio/.gitkeep

# Logs
*.log
logs/

# Celery
celerybeat-schedule
celerybeat.pid

# Pytest
.pytest_cache/
.coverage
htmlcov/
